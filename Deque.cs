using System;
using Queues;

namespace Queues
{
    public class Deque : IQueue
    {
        public void Dequeue(string itemId)
        {
            throw new NotImplementedException();
        }

        public void Enqueue<T>(T item)
        {
            throw new NotImplementedException();
        }
    }
}